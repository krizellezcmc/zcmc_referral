import React, { useState } from "react";
import api from "../API/Api";

function SearchDropwdown(props) {
  const [list, setList] = useState([]);
  const [selected, setSelected] = useState("");

  return <div></div>;
}

export default SearchDropwdown;
